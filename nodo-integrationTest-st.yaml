trigger:
- master



pool:
  vmImage: 'ubuntu-latest'



steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.6'



- script: python -m pip install --upgrade pip
  displayName: 'Install dependencies'



- script: python3 -m pip install -r requirements.txt
  displayName: 'Install requirements'



- script: |

    pwd
    ls -lr
    behave --junit-directory=report --junit -o report/index.html ./src/integ-test/bdd-test/features/NewMod3/primitives/activatePaymentNotice_syntax_ko.feature
  displayName: 'Run tests'

- task: PublishTestResults@2
  condition: succeededOrFailed()
  inputs:
    testResultsFiles: './report/test-*.xml'
    testRunTitle: 'Publish test results for Python $(python.version)'